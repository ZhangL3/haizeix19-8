#include <stdio.h>
#include <iostream>
using namespace std;

main() {
	int a, b, c;
	// scanf("%d%d%d", &a, &b, &c);
	// printf("%d\n", a * b * c);
	cin >> a >> b >> c;
	cout << a * b * c << endl;

	return 0;
}